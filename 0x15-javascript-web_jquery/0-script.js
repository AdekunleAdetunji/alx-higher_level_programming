#!/usr/bin/node
/*
This is a javascript file that updates the color of the header element color
to red using the DOM
*/
const header = document.querySelector('header');
header.setAttribute('style', 'color:#FF0000');
